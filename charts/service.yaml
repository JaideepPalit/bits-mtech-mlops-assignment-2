apiVersion: v1
kind: Service
metadata:
  name: heart-disease-service
  namespace: bits
spec:
  type: LoadBalancer
  selector:
    app: heart-disease-api
  ports:
    - port: 80
      targetPort: 8000
